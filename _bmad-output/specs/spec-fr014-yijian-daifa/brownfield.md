# 一键代发 · 与现有模块的边界

- **不是 FR-001 代发分配。** 那是 OPC 协作结算里的关键词/帐号分配（已暂停）。本增量是稿件市场：剪辑手供稿、用户领取。
- **不是 FR-002 代发收益。** 那是代发成员收益管理（已上线）。本增量详情只展示分成比例，广场与领取列表禁止展示收益金额，不沿用代发成员收益页。
- **不改 FR-012 选书中心、FR-013 AI申词 文件。** 领取消费的「该项目已通过关键关键词」假定复用现有申词审核结果，不重定义审核态。
- **回填复用该项目已有回填页。** 本 FR 只接领取后的入口并做必要调整，不重列字段、不另做一套回填表单。
- **新独立 FR-014。** 主入口按 ITERATION-FR-GUIDE：`demo/iteration/fr-{模块}-{需求}.html` 三 Tab（前端交互 / 管理后台 / 业务流程）+ 规则抽屉。建议文件名 `fr-opc-yijian-daifa.html`（模块随实现时再定，不锁死）。
- **交付约束（adopted：ITERATION-FR-GUIDE）：** C 端橙色；Mockup 只 iframe；Demo 不做真实接口/持久化；流程图手写 SVG，不用 Mermaid；不改 Sidebar 全局架构。
- **现有 Demo 里的「剪辑手」字样**（FR-001 成员角色）不是本增量的客服招募/后台录入体系，禁止复用那套分配记录当供稿/领取。
