---
topic: 活动中心 FR-006 v2
slug: fr006-activity-center
date: 2026-09-17
updated: 2026-09-21T14:02
---

- (direction) slug=fr006-activity-center；输入=demo/iteration/fr-activity-center.html + FR-006 v2 增量；产出 kernel + 后台字段表 + C端显隐规则。
- (note) project-context.md 不存在；未加载 persistent_facts 文件。
- (decision) v2 推翻 v1「本 FR 不重做详情页」：详情改为可配置容器。
- (decision) 详情 Tab 仅介绍/案例/榜单；后台配了才出现；默认落在第一个实际存在的 Tab；不做我的作品。
- (decision) 介绍 Tab 动态按钮 0–2 个；后台配文案；跳转类型 url 或 form（本活动新建/绑定表单）。
- (decision) 列表查看与详情按钮两套配置、不自动同步；列表有活动级外链则外跳并记已参与，无外链进详情；详情 0 个按钮则无底栏 CTA。
- (decision) 首页新增已参与 Tab；含表单提交以及纯外链点击（列表查看外链或详情 url 按钮）；同一用户同一活动去重；空态「暂无已参与的活动」；置顶 Banner 不随该 Tab 隐藏。
- (decision) 奖励类活动结束后展示该用户是否获奖：已获奖/未获奖/审核中；非奖励类不下发；未结束不展示结果。
- (decision) 榜单与奖励名单均支持人工导入，以及对接现有业务数据（后台按活动选数据源）；榜单仅已结束且有数据才下发 C 端。
- (constraint) 表单 v1 仅短文本/手机号/图片三种控件。
- (constraint) 对接业务数据只标映射占位，禁止发明本仓库不存在的接口。
- (constraint) v1 列表合同保留：图1卡片、置顶 Banner 最多1条且不随筛选隐藏、全部/进行中/已结束、浏览人数必带且前端不累加、参加人数虚拟未配整段不展示、后图仅跟在已配参加人数后、显示才进可见池、C端角标按时间算。
- (capability) CAP-1 运营配置活动基础信息与显示/隐藏；C 端只看到显示池内活动的 Banner+图1列表（含浏览/参加显隐）。
- (capability) CAP-2 详情为可配置 Tab 容器：介绍/案例/榜单按配置出现，默认第一个实际存在 Tab。
- (capability) CAP-3 介绍 Tab 0–2 动态按钮，文案与 url|form 由后台配置。
- (capability) CAP-4 列表查看外链与详情 CTA 独立配置；列表有外链外跳并记已参与，无外链进详情；详情 0 CTA 则无底栏。
- (capability) CAP-5 已参与：表单提交或外链点击记一次（用户×活动去重）；首页 Tab；空态文案固定；Banner 仍展示。
- (capability) CAP-6 奖励类结束后对本人下发已获奖/未获奖/审核中；非奖励与未结束不下发结果。
- (capability) CAP-7 榜单与奖励名单：人工导入或绑定现有业务数据源占位；榜单 C 端仅已结束且有数据。
- (capability) CAP-8 活动可新建或绑定一张表单，v1 字段类型仅短文本/手机号/图片。
- (assumption) 介绍 Tab 正文沿用 v1 活动内容富文本；无正文则介绍 Tab 视为未配置。
- (assumption) 列表「查看」文案固定，不随状态改；活动级外链即列表/Banner 查看的跳转，与详情按钮无关。
- (assumption) 一活动至多一张表单；多颗 form 按钮打开同一张已绑定表单。
- (assumption) 奖励类由活动级开关标记；关则永不下发获奖结果字段。
- (assumption) 已参与列表卡片结构同图1，且只含仍处于显示池的活动。
- (question) 案例 Tab 内容结构未拍板（富文本 / 案例条目列表）。
- (question) 榜单 C 端列结构未拍板。
- (question) 奖励名单导入字段与「审核中」写入方未拍板。
- (question) v1 编辑页无置顶字段；置顶开关落点未在 v2 重申。
- (question) 未登录外跳或提交是否先登录、能否记已参与未拍板。
- (question) 现有可绑定的业务数据源清单待映射，实现期不得发明接口名。
- (note) express 蒸馏：输入已含拍板增量与 v1 原型，未做逐字段 elicitation。
- (event) Pass1 Coherence: Spec Law 1-6+8 pass. CAP-1..8 均有 intent+success；Non-goals 明确；Success signal 可演示。
- (event) Pass2 Preservation: v1 列表/Banner/浏览参加/显示池/后台必填选填均入 kernel 或 companions；v2 六点拍板+表单三控件+数据源占位均入；推翻「不重做详情」已记。Wrapper-only: Demo CSS/RTE 工具条/Mock 行数据未写入合同。
- (event) Derived SPEC.md + glossary.md + admin-fields.md + visibility-rules.md + brownfield.md. Story breakdown 未跑（待用户决定）。
- (decision by user) 案例 Tab=多条案例条目；至少一条才出 Tab。单条内字段未点名。
- (decision by user) 榜单 C 端列仅：排名、用户头像、脱敏昵称。榜单是否展示由后台开关控制。此条覆盖先前「仅已结束且有数据才下发」。
- (decision by user) 奖励名单只经奖励管理写入。后台列：用户ID、用户昵称、活动排名、活动奖品/奖励金额、活动期间收益。不是 C 端第四 Tab。
- (decision by user) 置顶按 v1：活动编辑「是否置顶」；显示池内最多 1 条；取消立即撤 Banner。
- (decision by user) 参与与外跳走站内已有登录；本 FR 不重做登录、不单开未登录分支。
- (decision by user) 业务数据源映射、导入与数据源覆盖规则本轮不考虑（原开放问题 6、7 挂起）。
- (capability by user) CAP-7（修订）运营用开关控制 C 端是否展示榜单；展示时列为排名、头像、脱敏昵称。
- (capability by user) CAP-9 运营在奖励管理写入该活动奖励名单；后台表展示用户ID、昵称、活动排名、奖品/奖励金额、活动期间收益。
- (constraint by user) 脱敏昵称沿用站内已有用户展示规则，本 FR 不新造脱敏算法。
- (constraint by user) 本轮不对接现有业务数据源、不定义导入与数据源合并覆盖。
- (assumption by user) C 端已参与记账发生在已登录会话；登录能力视为已存在。
- (assumption by user) 奖励管理名单供后台查看与获奖相关数据，不直接等于 C 端榜单三列（榜单另受展示开关约束）。
- (note by user) 原 CAP-7 关于导入/数据源/已结束才下发的 success 被 2026-09-17 用户答复取代；ID 仍为 CAP-7。
- (question) 单条案例条目内字段（标题/封面/正文等）仍未拍板。
- (question) C端已获奖/未获奖/审核中 与奖励管理行如何对应仍未拍板。
- (question) 榜单行数据写入方本轮挂起；开关只控制是否展示已有行。
- (assumption) leaderboardVisible 打开但无行：榜单 Tab 仍出现，空列表；未另定空态文案。
- (event) Pass1 Coherence: CAP-1..9 intent+success；CAP-7 ID 保留并修订；Non-goals 含本轮不对接合登录；Success signal 已去掉「未结束看不到榜单」。
- (event) Pass2 Preservation: 用户1多条案例 2榜单三列+后台开关 3奖励管理五列 4置顶v1 5登录不改 6-7挂起 均入合同。覆盖「已结束且有数据才下发榜单」。Wrapper-only: 无。
- (event) Re-derived SPEC.md + companions. 未改 demo/iteration/fr-activity-center.html；可按用户意愿回写原型。
- (decision by user) 案例不配标题/封面/正文等字段；后台用视频上传按钮，多条=多个视频；至少 1 个视频才出案例 Tab。
- (decision by user) 奖励管理与活动按活动ID对应：名单行归属该活动ID；C端获奖结果按当前活动ID查奖励管理。
- (note by user) 用户追问「榜单每一行数据从哪写入是什么意思」：该问指 C 端三列的人从哪张表来，不是问展示开关。未确认是否=该活动ID的奖励管理名单投影。不得静默选定。
- (capability by user) CAP-2（修订）案例 Tab=已上传视频列表，无条目字段合同。
- (capability by user) CAP-6/CAP-9（修订）奖励名单与获奖查询主键为活动ID。
- (constraint by user) 禁止为案例发明标题/封面/正文等字段并当作合同。
- (question by user) C端榜单三列的行是否直接投影该活动ID的奖励管理名单（排名+用户头像+脱敏昵称）？若否，另有写入面。
- (question by user) 审核中如何在奖励管理中表达；仅有活动ID关联尚未给出三态算法。
- (event) Pass1: 案例改为视频上传无字段；CAP-6/9 以活动ID关联。未静默把榜单行写成奖励管理。
- (event) Pass2: 用户「按钮是视频上传不需要具体字段」「根据活动ID对应奖励管理」已入合同。榜单行来源改写成可回答的确认项。Wrapper-only: 无。
- (decision by user) 榜单行=该活动ID在现有奖励管理中的名单，C端只投影排名、头像、脱敏昵称。不另建榜单表。
- (decision by user) 奖励管理是现有业务流程，本 FR 不建设、不改造、不验收其录入与表结构；只按活动ID只读消费。
- (capability) CAP-9 废止（ID 永不复用）：不再作为本增量交付。读现有奖励管理并入 CAP-6/CAP-7。
- (constraint) 禁止本 FR 新增或改奖励管理页/写入接口/名单表；禁止发明其 HTTP 路径。
- (assumption) 获奖三态算法属现有奖励管理；本 FR 只展示其按用户×活动ID 返回的结果。
- (event) Pass1: CAP-9 废止仍保留 ID 与 intent+success；Open Questions 清空；CAP-6/7 改为只读现有奖励管理。
- (event) Pass2: 「是」=榜单行即该活动奖励管理投影；「奖励管理不需要考虑」=本 FR 不建设该流程。覆盖 CAP-9 写入合同与五列验收。Wrapper-only: 无。
- (decision) 详情标签与列表一致为项目·{project}；去掉@与收藏；最高收益改为结束后是否获奖+奖品/金额；介绍加长mock；案例HTML5可播；榜单营销向样式仍三列。
- (decision) 详情顶栏加浏览/参加人数；获奖条三套文案（未获奖安慰、奖金恭喜、实物恭喜）；榜单第一名冠军卡，列表从第二名起展示奖品/奖金；列表去掉参加人数后图；已参与列表显示已获奖/未获奖角标。
- (decision) 后台新增/编辑改为两栏：左字段含介绍正文富文本（不再单独活动内容栏），右保留C端详情预览；去掉参加人数后图；加详情封面；榜单文案对齐冠军卡+奖品。
- (decision) 后台编辑布局对齐预估数据：左配置1fr、右预览固定360px、手机框约320px且更高；字段两列排布；预览头为前端预览+实时同步。
- (decision by user) 后台配置调整为三组：基础信息（标题、标签、平台、赛道取现有 list、项目取现有 list 多选、周期、浏览量、参加人数）、活动跳转 1/2（名称+地址，未配前端不显示按钮）、活动详情（介绍富文本、案例图+视频附件、榜单开/关）。
- (event) 回写 admin-fields / visibility-rules / Demo 编辑页与 C 端跳转显隐。
- (decision) 废止活动级外链：列表/Banner「查看」一律进详情，不外跳、不因该点击记已参与。已参与仅表单提交成功或点击详情已配置跳转。去重仍 (user, 活动)。
- (decision) FR Demo 对照面按 FR-014：顶层 Tab 改为前端交互 / 管理后台 / 前后端数据交互；前端二级场景/业务流程/开发提示词；后台二级列表/业务流程/开发提示词。旧 ?tab=flow 兼容落到前端业务流程。
- (capability) CAP-4（修订）查看只进详情；详情 CTA 独立；废止 listOutboundUrl。
- (capability) CAP-5（修订）已参与不再包含列表外链点击。
- (event) 回写 SPEC CAP-4/5、Constraints、Success signal、glossary「活动级外链」、visibility-rules §3/§5；iteration 与 ux mockups 同步。
- (event) FR Demo 补全场景全屏预览与端到端业务流程；合同仍以 SPEC / visibility-rules / admin-fields 为准，未发明活动级外链或奖励管理 HTTP
- (decision by user) 后台 Demo 子菜单按数据域拆：活动管理(CRUD) / 报名表单(CAP-8) / 已参与(CAP-5) / 榜单只读(admin-fields §4)。不建设奖励管理。
- (decision by user) 后台不拆子菜单。参加人数已配置可点进参与详情。操作=编辑/达标用户/删除。达标用户奖励计算按活动奖励规则，不建设奖励管理。
- (decision by user) 后台新增/编辑改为三步：1基础信息+活动介绍；2活动报名；3活动奖励规则（规则待定）。
- (decision by user) 案例附件与榜单开关节配置在第1步，与介绍一起，不挪到报名步或奖励步。
- (decision by user) 报名可混配：同一活动可同时配置表单字段与跳转链接，非互斥、非按条选 url|form。
- (decision by user) 表单本轮增控件：下拉框、图片、日期。用户选的是「增」并点名这三类。
- (decision by user) 第3步仅空态占位：有步骤、不可填写、不写入保存模型；禁止做成可配开关/标题。
- (capability) CAP-8（修订）运营在第2步配置本活动表单字段；可与跳转同时下发；本轮点名控件含下拉框、图片、日期。
- (capability) CAP-10 运营进入新增/编辑第3步只看到活动奖励规则空态占位；该步无字段、不可填、不验收规则引擎。
- (constraint) 新增/编辑必须三步，禁止改回单页平铺三组；第1步含现有基础信息+介绍+案例+榜单；第2步表单与跳转可同时配；第3步空态。
- (constraint) 详情底栏跳转仍为名称+地址，不是 form 类型按钮；表单是第2步独立配置，不恢复按条 url|form。
- (constraint) 禁止把第3步做成可保存的奖励规则或开关/标题；禁止本轮验收奖励规则计算。
- (assumption) 第1步基础信息字段集合不变：标题/标签/平台/赛道/项目/周期/浏览量/参加人数/显示隐藏/置顶。
- (assumption) 跳转仍 0–2 条；一条已配置=名称与地址都非空。用户未改条数。
- (assumption) 一活动至多一张表单；第3步不产生任何 C 端下发字段。
- (question) 短文本、手机号是否仍为合法表单控件？用户选增控件但只点名下拉框/图片/日期。
- (question) 下拉框选项如何配置（手填选项列表 vs 其他）未拍板，不得发明选项数据源接口。
- (question) 表单与跳转同时配置时，C 端表单渲染位置与提交按钮文案未拍板。
- (event) Pass1 Coherence: Spec Law 1-6+8 pass. CAP-1..10 均有 intent+success；CAP-9 废止 ID 保留不复用；CAP-10 为空态占位；Non-goals 含不互斥、不填奖励规则；Success signal 可演示三步。
- (event) Pass2 Preservation: 1-A案例榜单在第1步 2-B报名可混配 3-B增下拉/图片/日期 4-A第3步空态 均入 kernel+admin-fields+visibility-rules。未发明短文本/手机号去留、下拉选项源、混配表单位置。Wrapper-only: 无。
- (event) Re-derived SPEC.md + admin-fields.md + visibility-rules.md + glossary.md + brownfield.md。未改 demo。无 stories.yaml。
- (decision by user) 现役模型定为 B（companions）：两步向导、领取按钮、案例名称+组成、第2步可配奖励规则；覆盖 kernel 三步空态/名称地址跳转/独立表单。
- (decision by user) 去掉榜单：后台不配榜单开关/条数，C 端不展示榜单 Tab、颁奖台与名单投影。
- (capability) CAP-7 废止（ID 永不复用）：本 FR 不交付榜单配置与 C 端展示。
- (capability) CAP-2 修订：详情 Tab 仅介绍/案例已配置子集；默认第一个实际存在 Tab；禁止我的作品；无榜单。
- (capability) CAP-3 修订：领取开关 + 最多 2 个按钮；需跳转则文案+地址；不需跳转则文案+点击后变更文案；半填不下发；关或 0 条无底栏。
- (capability) CAP-5 修订：已参与仅点击已配置领取按钮（跳转或文案变更）；查看不记；去重 (user, 活动)。
- (capability) CAP-8 废止（ID 永不复用）：不交付报名表单字段配置与提交面；被领取按钮取代。
- (capability) CAP-10 废止（ID 永不复用）：不再是第3步奖励规则空态。
- (capability) CAP-11：运营在第2步添加 0–N 条奖励规则（播放奖/全勤奖/累计项目收益奖）；不完整不下发。
- (constraint) 新增/编辑必须两步：1 基础信息+介绍+案例（无榜单）；2 活动领取+奖励规则。禁止三步空态，禁止改回三组平铺。
- (constraint) 禁止活动编辑出现 leaderboardVisible/leaderboardLimit；禁止 C 端介绍/案例之外再出榜单 Tab。
- (constraint) 禁止把领取做成 form 类型按钮；禁止恢复按条 url|form；禁止独立报名表单。
- (assumption) 奖励类开关 isReward 在第1步基础信息；关则永不下发获奖结果。
- (assumption) rewardRules 仅完整规则下发；C 端不展示规则正文；达标用户奖励计算按本活动规则，不建设奖励管理录入页。
- (assumption) 案例 Tab 按条渲染名称+图片或视频（视频 HTML5 可播）；覆盖「只展示视频」。
- (assumption) 已参与列表角标仅已获奖/未获奖；审核中不画该角标，详情仍可按 CAP-6 下发三态。
- (question) 详情顶栏左封面（88px）是否有后台字段？未点名前不得发明 cover URL。
- (question) 待开始或已结束时领取按钮是否可点、失败跳转是否仍记已参与？
- (assumption) 后台列表「排序」列为展示，本 FR 不新增独立排序写入字段，沿用既有列表序。
- (event) Pass1 Coherence: Spec Law 1-6+8 pass. CAP-1..11 均有 intent+success；CAP-7/8/10 废止 ID 保留；CAP-11 为第2步奖励规则；Non-goals 含不配榜单不交表单；Success signal 可演示两步且无榜单。
- (event) Pass2 Preservation: 用户选 B + 去掉榜单配置与 C 端展示均入 kernel+companions。领取按钮/案例名称+组成/两步规则/isReward/已参与=领取点击已入。Wrapper-only: Demo HTML 为 adopted companion 本轮未改；封面与期外领取仍 Open Questions。
- (decision by user) 详情顶栏封面使用后台配置字段「封面」cover；未下发则不展示封面。
- (decision by user) 待开始或已结束不能点领取；按钮可展示但不可点，该点击不记已参与。
- (decision by user) 不需跳转的领取：点击即记已参与。需跳转的领取：仅跳转成功才记；跳转失败不记。
- (decision by user) 去掉获奖字段：C 端不下发、不展示已获奖/未获奖/审核中、强调条与已参与结果角标；删除 isReward。
- (capability) CAP-6 废止（ID 永不复用）：本 FR 不交付获奖结果字段与展示。
- (constraint) 禁止 C 端下发或展示获奖结果、奖励类开关；禁止只读现有奖励管理做本人结果条。
- (assumption) 封面为图片；详情顶栏左侧按 88px 展示已下发 cover；列表/Banner 不强制用封面。
- (event) Pass1 Coherence: Spec Law 1-6+8 pass. CAP-6 废止获奖字段；CAP-2 封面；CAP-3/5 期外不可点、跳转失败不记、点击领取可记。Open Questions 已清空。
- (event) Pass2 Preservation: 用户四点（封面字段、期外不可领、跳转失败不记且点击领取可记、去掉获奖字段）均入 kernel+companions。删除 isReward 与结果强调条。Wrapper-only: Demo HTML 仍为 adopted 未改。
- (event) 回写 Demo：C 端封面/期外不可领/跳转失败不记账；FR 去掉获奖场景与榜单；impl-prompts 与 e2e 对齐 SPEC；ux mockups 同步。
- (event) 还原第1步基础信息配置控件：标题字数、标签回车芯片、时间占位、浏览量步进、封面上传、介绍富文本；字段集合不变。
- (decision by user) 后台预览详情：介绍与案例都有完整数据才显示 Tab；无完整案例时不显示 Tab，默认展示活动介绍。
- (decision by user) 还原后台编辑：播放/名次按阶梯行配置（阈值+奖品+删除+添加阶梯）；案例可删除。
- (change by user) 编辑页布局参考卡片分区截图收紧：基础信息/详情内容、领取/奖励规则分卡片；阶梯行带列头，案例与阶梯仍可删。
- (decision) 用户要求详情内容增加榜单 Tab：开启/隐藏；开启后配 TOP 前 x 名；C 端固定格式（颁奖台前3 + 列表）列仅为 rank/avatarUrl/nicknameMasked/value；value 由能力「活动榜单」按活动统计规则返回展示串。覆盖先前「去掉榜单 / CAP-7 废止」。
- (decision) 用户：榜单改为参考列表截图、无颁奖台；详情 Demo 需三 Tab 与仅介绍无 Tab；信息卡片与 Tab 分区；有榜单活动展示已获奖/未获奖展位。覆盖颁奖台与「无获奖字段」。
- (event) FR Demo 前端场景收口为列表内切筛选 + 三 Tab 已获奖；Tab/领取/奖品展位/其他规则改在场景左侧列出。
- (decision) description 与 tags[] 分开下发；本人展位改由 showUserAward 控制，覆盖「仅有榜单才下发 awardStatus」。详情内容配置不展示该样式。CAP-6 沿用编号。
- (decision) 活动榜单增加 badges[] 与 self；领取仅介绍Tab；案例图放大/长按保存、视频可播。播放阶梯 playsFrom~playsTo + prizeKind/prizeValue；收益奖 rankFrom~rankTo + excludeProjects[]。
- (override) 覆盖能力出参 metricLabel/periodLabel/badges[]：改由后台 leaderboardLineDesc、leaderboardPeriodDesc 下发；行仅脱敏昵称无标签。
- (decision) 累计项目收益奖去掉 excludeProjects；参与范围改为 brandProjects[]（品牌项目 list 多选，含番茄小说、知乎故事及全部品牌项目）与 otherBizProjects[]（商单、版权、融合短剧、融合小说、融合漫剧、海外小说、海外短剧、快手零粉、快手星火多选）；两类至少一类。播放奖/名次阶梯字段与控件同一行不换行。
- (decision) 后台详情内容：案例卡片两列平铺；榜单 TOP 与行描述同一行。C 端案例一行一条、不并排。榜单描述字段名即后台 leaderboardLineDesc/leaderboardPeriodDesc，灰色小字不高亮；昵称与 value 与头像垂直居中。
- (event) 交付开发：新增 fe-be-contract.md；闭合隐藏深链、待开始筛选、无介绍无底栏、跳转调起、展位与榜单解耦、规则命中顺序；修正棕地/admin-fields 把获奖绑榜单的矛盾。
